import { TagCloud } from "@jackyzha0/quartz"

// Top tags by post count, as mounted on the mobile home page
// (quartz.layout.ts:9 — limit 8). Counts come from the real content/ tags.
export const Default = () => <TagCloud />
