import { Explorer } from "@jackyzha0/quartz"

// Collapsible file tree. The tree is filled in by its inline script at
// runtime, so the card shows the header and mount point.
export const Default = () => <Explorer />
