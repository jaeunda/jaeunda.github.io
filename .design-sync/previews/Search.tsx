import { Search } from "@jackyzha0/quartz"

// Search trigger in the sidebar. The overlay itself opens from the inline
// script, so the card shows the resting button.
export const Default = () => <Search />
