import { Hero } from "@jackyzha0/quartz"

// The home hero. Renders only on the index page, so it is bound to the index
// fixture; the greeting/accent/subtitle come from the component's options.
export const Default = () => <Hero />
