import { TagCloudSidebar } from "@jackyzha0/quartz"

// The same component with the sidebar options quartz.layout.ts:57 uses
// (variant "sidebar", showOnAllPages) — a denser stack for the left rail.
export const Default = () => <TagCloudSidebar />
